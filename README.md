#Node Web Scrapper#
===================

This node module is designed to make HTTP GET requests to remote web pages
and download their HTML. It will then parse the HTML and write the content
the user is interested in to a file on disk. 
